const COLORS = <const> ['red', 'blue', 'green', 'gray', 'pink'];
type Colors = typeof COLORS[number];

const SWAP_BIN_COLORS_INTERVAL_MS = 40000;
const MIN_BOMB_LIFE_TIME_MS = 5000;
const MAX_BOMB_LIFE_TIME_MS = 10000;
const BOMBS_COUNT_TILL_GAME_END = 120;

const START_BOMB_SPWAN_INTERVAL_MS = 5000;
const END_BOMB_SPWAN_INTERVAL_MS = 500;
